{% assign first_collection = product.collections.first %}

{% comment %}
  Load the same CSS as main-collection-product-grid for consistent styling.
  (Remove any duplicates if these assets are already loaded globally.)
{% endcomment %}
{{ 'template-collection.css' | asset_url | stylesheet_tag }}
{% unless section.settings.quick_add == 'none' %}
  {{ 'quick-add.css' | asset_url | stylesheet_tag }}
{% endunless %}
{{ 'swatches-cheyenne.css' | asset_url | stylesheet_tag }}
{{ 'component-card.css' | asset_url | stylesheet_tag }}
{{ 'component-price.css' | asset_url | stylesheet_tag }}

{%- comment -%} Load size drawer and calculator drawer assets {%- endcomment -%}
{{ 'size-drawer.css' | asset_url | stylesheet_tag }}
{{ 'size-calculator-drawer.css' | asset_url | stylesheet_tag }}
<script src="{{ 'size-drawer.js' | asset_url }}" defer="defer"></script>
<script src="{{ 'size-calculator-drawer.js' | asset_url }}" defer="defer"></script>

{% if section.settings.image_shape == 'blob' %}
  {{ 'mask-blobs.css' | asset_url | stylesheet_tag }}
{% endif %}

{% if section.settings.quick_add == 'standard' %}
  <script src="{{ 'quick-add.js' | asset_url }}" defer="defer"></script>
  <script src="{{ 'product-form.js' | asset_url }}" defer="defer"></script>
{% endif %}

{% if section.settings.quick_add == 'bulk' %}
  <script src="{{ 'quick-add-bulk.js' | asset_url }}" defer="defer"></script>
  <script src="{{ 'quantity-popover.js' | asset_url }}" defer="defer"></script>
  <script src="{{ 'price-per-item.js' | asset_url }}" defer="defer"></script>
  <script src="{{ 'quick-order-list.js' | asset_url }}" defer="defer"></script>
{% endif %}

<style>
  /* Minimal styling for the grid layout and responsiveness */
  @media screen and (max-width: 749px) {
    #product-grid {
      width: 100%;
      display: flex;
    
    }
    .grid {
      column-gap: 2%;
    }
    #product-grid .grid__item {
      width: 49%;
    }
    .grid__item {
      max-width: 49%;
    }
    #shopify-section-template--24389098570052__product-grid > div > div.collection-header.page-width > h1 {
      margin-left: 20px;
      font-weight: 600;
      font-size: 16px;
      margin-bottom: 0!important;
    }

  }
  @media screen and (min-width: 750px) {
    #product-grid.random-grid {
      display: grid !important;
      grid-template-columns: repeat(4, 1fr) !important;
      column-gap: 1.6rem;
      row-gap: 2rem;
      padding-left: 1.6rem;
      padding-right: 1.6rem;
      padding-inline-start: 1.6rem !important;
      padding-inline-end: 1.6rem !important;
    }
    #product-grid.random-grid .grid__item {
      display: flex;
      flex-direction: column;
      width: 100% !important;
      max-width: 100% !important;
      box-sizing: border-box;
      align-self: start;
      height: 100%!important;
      padding-left: 0 !important;
      padding-bottom: 0 !important;
    }
    #shopify-section-template--24389098570052__product-grid > div > div.collection-header.page-width > h1 {
      margin-left: 25px;
      font-weight: 600;
      font-size: 20px;
      line-height: 18px;
    }
  }

  
  /* Optional additional layout options */
  .product-grid.two-col {
    grid-template-columns: repeat(2, 1fr) !important;
  }
  .product-grid.mixed-layout {
    grid-template-columns: repeat(2, 1fr) !important;
  }
  .product-grid.mixed-layout li:nth-child(3n+1) {
    grid-column: span 2;
  }
  .grid-controls {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  .layout-options .layout-option-button {
    border: none;
    background: #fff;
    cursor: pointer;
  }
  .layout-options .layout-option-button.active {
    background: transparent;
    color: #fff;
  }
  .facets-toggle-button {
    border: 1px solid #ccc;
    background: #fff;
    cursor: pointer;
  }
  .facets-toggle-button.active {
    background: #000;
    color: #fff;
  }
  .layout-option-button .grid-icon {
    width: 22px;
    height: 22px;
    display: inline-block;
  }
  .layout-option-button.active .grid-icon--full {
    display: inline-block;
  }
  .layout-option-button.active .grid-icon--empty {
    display: none;
  }
  .layout-option-button:not(.active) .grid-icon--full {
    display: none;
  }
  .layout-option-button:not(.active) .grid-icon--empty {
    display: inline-block;
  }
  #main-collection-filters > div.facets-container.facets-container-drawer > menu-drawer > details > summary > span.mobile-facets__open {
    display: none; 
  }
  #shopify-section-template--24389098570052__product-grid > div > div.grid-controls.page-width > div.facets-toggle-container {
    margin-right: 14px;
  }
  .layout-options {
    margin-left: 14px;
  }
  #facets-toggle-button > span > svg {
    width: 28px;
    height: 22px;
    display: inline-block;
  }
  #facets-toggle-button {
    border: none;
  }
  #shopify-section-template--24389098570052__product-grid > div > div.grid-controls.page-width > div.facets-toggle-container {
    margin-right: 14px;
  }

  /* Match quick add size spacing to the standard drawer */
  .section-random-collection-padding .product-card-plus .overlay-sizes {
    display: flex !important;
    flex-direction: column !important;
    gap: 8px;
  }

  .section-random-collection-padding .product-card-plus .overlay-sizes > .size-option {
    border: none;
    background: none;
    padding: 16px 0 !important;
    font-size: 14px;
    font-family: "Work Sans";
    font-weight: 400;
    cursor: pointer;
    border-radius: 0;
    width: 100%;
    text-align: center;
    display: flex !important;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    gap: 4px;
    height: auto !important;
  }

  .section-random-collection-padding .product-card-plus .overlay-sizes > .size-option:not(.size-option--recommended) {
    background: none;
  }

  .section-random-collection-padding .product-card-plus .overlay-sizes > .size-option.size-option--recommended {
    background-color: #f2f2f2 !important;
    border-color: rgba(0, 0, 0, 0.12);
  }

  .section-random-collection-padding .product-card-plus .overlay-sizes > .size-option + .size-option {
    margin-top: 0;
  }
</style>

{% comment %}
  Wrap the grid in an existing container and add an ID so the swatch click functionality can target it.
  (This should not ruin your layout if your theme’s markup already uses these classes.)
{% endcomment %}
{% if first_collection == blank or first_collection.products.size == 0 %}
  <div class="collection collection--empty page-width" id="product-grid">
    <div class="loading-overlay gradient"></div>
    <div class="title-wrapper center">
      <h2 class="title title--primary">
        No related products found.
      </h2>
    </div>
  </div>
{% else %}
  <div class="section-random-collection-padding gradient color-scheme-1">
    <div class="collection page-width" id="ProductGridContainer">
      <div class="loading-overlay gradient"></div>
      <ul id="product-grid" class="grid product-grid random-grid">
        {%- assign skip_card_product_styles = false -%}
        {% assign index = 0 %}
        {% for related_product in first_collection.products limit: 13 %}
          {% if related_product.id != product.id %}
            {% assign index = index | plus: 1 %}
            {% assign lazy_load = false %}
            {% if index > 2 %}
              {% assign lazy_load = true %}
            {% endif %}
            <li class="grid__item">
              {% render 'card-product',
                card_product: related_product,
                media_aspect_ratio: 'portrait',
                image_shape: 'default',
                show_secondary_image: false,
                show_vendor: false,
                show_rating: false,
                lazy_load: lazy_load,
                skip_styles: skip_card_product_styles,
                quick_add: 'none',
                section_id: 'product-grid'
              %}
            </li>
            {% assign skip_card_product_styles = true %}
          {% endif %}
        {% endfor %}
      </ul>
    </div>
  </div>
{% endif %}

<!-- Swatch Click Script for Random Collection Grid -->
<script>
  // Attach a document-level click listener for swatch clicks inside product cards.
  document.addEventListener('click', function(e) {
    var swatchEl = e.target.closest('.swatch');
    if (!swatchEl) return;
    var card = swatchEl.closest('.product-card-wrapper');
    if (!card) return;
    
    // Remove the active state from all swatches in this card.
    card.querySelectorAll('.swatch').forEach(function(el) {
      el.classList.remove('active');
    });
    swatchEl.classList.add('active');
    
    // Get the new image URL from data-variant-image.
    var newImage = swatchEl.getAttribute('data-variant-image');
    if (!newImage) return;
    
    // Update the card's featured image.
    var mediaContainer = card.querySelector('.card__media');
    if (!mediaContainer) return;
    var img = card.querySelector('.swiper-slide-active img') || mediaContainer.querySelector('img');
    if (img) {
      img.src = newImage;
      img.srcset = newImage;
    }
  });
</script>
