{% comment %}
  Wishlist Sync Banner - Shows for guest users
  Informs them that favorites aren't saved across devices
{% endcomment %}

{% unless customer %}
  <div class="wishlist-sync-banner" id="wishlist-sync-banner">
    <div class="wishlist-sync-banner__content">
      <svg class="wishlist-sync-banner__icon" width="20" height="20" viewBox="0 0 20 20" fill="none">
        <path d="M10 0C4.48 0 0 4.48 0 10C0 15.52 4.48 20 10 20C15.52 20 20 15.52 20 10C20 4.48 15.52 0 10 0ZM11 15H9V13H11V15ZM11 11H9V5H11V11Z" fill="currentColor"/>
      </svg>
      <div class="wishlist-sync-banner__text">
        <strong>Os seus favoritos não são guardados entre dispositivos.</strong>
        <span>Inicie sessão para sincronizar a sua lista em todos os seus dispositivos.</span>
      </div>
      <a href="{{ routes.account_login_url }}" class="wishlist-sync-banner__button">
        Iniciar sessão
      </a>
      <button type="button" class="wishlist-sync-banner__close" aria-label="Fechar">
        <svg width="14" height="14" viewBox="0 0 14 14" fill="none">
          <path d="M14 1.41L12.59 0L7 5.59L1.41 0L0 1.41L5.59 7L0 12.59L1.41 14L7 8.41L12.59 14L14 12.59L8.41 7L14 1.41Z" fill="currentColor"/>
        </svg>
      </button>
    </div>
  </div>

  <style>
    .wishlist-sync-banner {
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      color: #ffffff;
      padding: 1rem;
      margin-bottom: 1.5rem;
      border-radius: 0.5rem;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
      animation: wishlist-banner-slide-down 0.3s ease-out;
    }

    @keyframes wishlist-banner-slide-down {
      from {
        opacity: 0;
        transform: translateY(-10px);
      }
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }

    .wishlist-sync-banner.hidden {
      display: none;
    }

    .wishlist-sync-banner__content {
      display: flex;
      align-items: center;
      gap: 1rem;
      max-width: 1200px;
      margin: 0 auto;
    }

    .wishlist-sync-banner__icon {
      flex-shrink: 0;
      opacity: 0.9;
    }

    .wishlist-sync-banner__text {
      flex: 1;
      display: flex;
      flex-direction: column;
      gap: 0.25rem;
    }

    .wishlist-sync-banner__text strong {
      font-size: 0.9375rem;
      font-weight: 600;
    }

    .wishlist-sync-banner__text span {
      font-size: 0.875rem;
      opacity: 0.9;
    }

    .wishlist-sync-banner__button {
      background: rgba(255, 255, 255, 0.2);
      color: #ffffff;
      padding: 0.5rem 1.25rem;
      border-radius: 0.375rem;
      text-decoration: none;
      font-size: 0.875rem;
      font-weight: 600;
      white-space: nowrap;
      transition: background 0.2s ease;
      border: 1px solid rgba(255, 255, 255, 0.3);
    }

    .wishlist-sync-banner__button:hover {
      background: rgba(255, 255, 255, 0.3);
      color: #ffffff;
    }

    .wishlist-sync-banner__close {
      background: none;
      border: none;
      color: #ffffff;
      cursor: pointer;
      padding: 0.5rem;
      opacity: 0.8;
      transition: opacity 0.2s ease;
      flex-shrink: 0;
    }

    .wishlist-sync-banner__close:hover {
      opacity: 1;
    }

    @media screen and (max-width: 749px) {
      .wishlist-sync-banner__content {
        flex-wrap: wrap;
        gap: 0.75rem;
      }

      .wishlist-sync-banner__text {
        flex-basis: 100%;
        order: 1;
      }

      .wishlist-sync-banner__icon {
        order: 0;
      }

      .wishlist-sync-banner__button {
        order: 2;
        flex: 1;
        text-align: center;
      }

      .wishlist-sync-banner__close {
        order: 3;
      }

      .wishlist-sync-banner__text span {
        font-size: 0.8125rem;
      }
    }
  </style>

  <script>
    // Allow users to dismiss the banner
    (function() {
      const banner = document.getElementById('wishlist-sync-banner');
      if (!banner) return;

      const closeButton = banner.querySelector('.wishlist-sync-banner__close');
      if (!closeButton) return;

      // Check if user previously dismissed the banner
      const dismissed = sessionStorage.getItem('wishlist-sync-banner-dismissed');
      if (dismissed === 'true') {
        banner.classList.add('hidden');
      }

      closeButton.addEventListener('click', function() {
        banner.classList.add('hidden');
        sessionStorage.setItem('wishlist-sync-banner-dismissed', 'true');
      });
    })();
  </script>
{% endunless %}
