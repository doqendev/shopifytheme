# Porta local do servidor
PORT=5055

# O App secret da tua app Shopify (Partner Dashboard > App setup)
APP_SECRET=cc6a8a6c43a300dae299270586201ae1

# Caminho opcional para a BD SQLite (por defeito: ./wishlist.sqlite)
SQLITE_PATH=./wishlist.sqlite

# Modo de verificação da assinatura do App Proxy:
#  - sha256  (moderno)  -> HMAC-SHA256 nos parâmetros ordenados
#  - md5     (legacy)   -> md5(appSecret + concatenatedQueryWithoutSignature)
APP_PROXY_MODE=sha256

# (Opcional) Domínio público do teu servidor (para referência/logs)
PUBLIC_BASE_URL=https://your-public-server-domain.example
